<Page
    x:Class="WebCamImageCollector.RemoteControl.UI.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WebCamImageCollector.RemoteControl.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <Style TargetType="StackPanel">
            <Setter Property="Margin" Value="5,10,5,10" />
        </Style>
    </Page.Resources>
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="btnSave" Label="Save" Click="btnSave_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE74E;" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton x:Name="btnCancel" Label="Cancel" Click="btnCancel_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE711;" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>
    </Page.BottomAppBar>
    <StackPanel>
        <StackPanel>
            <TextBlock Text="URL:" />
            <TextBox x:Name="tbxUrl" />
        </StackPanel>
        <StackPanel>
            <TextBlock Text="Authentication token:" />
            <TextBox x:Name="tbxAuthenticationToken" />
        </StackPanel>
    </StackPanel>
</Page>
